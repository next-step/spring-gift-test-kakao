# 테스트 전략

## 테스트 범위와 검증

1. **인수 테스트(Acceptance Test)만 작성한다.**
    - API 레벨의 인수 테스트를 작성한다. 단위 테스트는 작성하지 않는다.
    - 리팩토링을 하더라도 테스트 코드를 수정할 필요가 없어야 한다.
    - 내부 구현이 아닌 외부 동작(HTTP 요청/응답)을 검증한다.
2. **최소 5개 이상의 행위를 검증한다.**
    - 검증할 행위의 기준은 API 엔드포인트이다.
3. **HTTP 계약, 관찰 가능한 상태 결과, 사이드 이펙트를 검증한다.**
    - DB를 직접 조회하기보다 API를 통해 확인한다.

## 시나리오 작성

4. **Cucumber BDD(Gherkin)로 시나리오를 작성한다.**
    - 한글 Given-When-Then으로 비개발자도 이해할 수 있게 작성한다.
    - Step Definitions는 재사용 가능하게 설계한다.
    - Cucumber 테스트 생성 시 `/cucumber-test` 스킬을 사용한다.
5. **시나리오명은 조건-결과 형식으로 작성한다.**
    - 조건과 결과가 드러나도록 작성하여, 시나리오명만으로 무엇을 검증하는지 파악 가능해야 한다.
    - 예: "유효한 이름이면 카테고리가 생성된다", "재고 초과 시 선물 전달이 실패한다"

## 테스트 환경

6. **Production Parity를 추구한다.**
    - 프로덕션과 동일한 DB(PostgreSQL)로 테스트한다.
    - in-memory DB는 프로덕션과 동작 차이가 있어 신뢰도가 떨어진다.
7. **테스트 환경은 컨테이너로 자동화한다.**
    - DB와 애플리케이션을 Docker Compose로 구성한다.
    - 개인 로컬 설정에 의존하지 않고, 누구나 동일한 명령으로 실행 가능해야 한다.

## 데이터 관리

8. **시나리오 간 데이터는 완전히 격리한다.**
    - 각 시나리오 실행 전 DB를 초기화하여 테스트 간 간섭을 방지한다.
9. **테스트 데이터는 시나리오의 사전 조건(Given)에서 준비한다.**
    - API가 있는 데이터는 API 호출로 준비한다.
    - API가 없는 데이터는 직접 DB 접근 또는 Fixture로 준비한다.
