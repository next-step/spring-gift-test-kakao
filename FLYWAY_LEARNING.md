# Flyway DB 마이그레이션 학습 기록

## 1. 왜 Flyway를 도입하는가?

### 문제: ddl-auto로 스키마를 관리하는 한계

기존에는 `spring.jpa.hibernate.ddl-auto=update`로 Hibernate가 스키마를 자동 생성/변경했다.

```yaml
# docker-compose.yml (변경 전)
environment:
  SPRING_JPA_HIBERNATE_DDL_AUTO: update
```

이 방식의 문제점:

| 문제 | 설명 |
|------|------|
| 스키마 버전관리 불가 | 어떤 변경이 언제 적용됐는지 추적할 수 없음 |
| 환경 간 불일치 | 개발/테스트/운영에서 서로 다른 스키마가 만들어질 수 있음 |
| 위험한 자동 변경 | `update`는 컬럼 추가는 하지만 삭제/이름변경은 못함. 운영에서 예측 불가 |
| 책임 분리 위반 | "스키마를 어떻게 만들지"는 애플리케이션/데이터 계층의 책임인데, Docker Compose 환경변수로 관리하고 있었음 |

### 해결: Flyway로 스키마를 코드로 관리

Flyway는 SQL 마이그레이션 스크립트를 버전 순서대로 실행하여 DB 스키마를 관리하는 도구다.
스키마 변경이 코드(SQL 파일)로 관리되므로, Git으로 이력 추적이 가능하다.

| 항목 | ddl-auto=update | Flyway |
|------|-----------------|--------|
| 스키마 정의 | Hibernate가 엔티티에서 추론 | 개발자가 SQL로 명시 |
| 버전 관리 | 불가 | V1, V2, V3... 순서대로 관리 |
| 변경 이력 | 없음 | `flyway_schema_history` 테이블에 기록 |
| 환경 일관성 | 보장 안 됨 | 모든 환경에서 동일한 스크립트 실행 |
| 업계 표준 | 개발/테스트 용도 | 프로덕션 표준 |

---

## 2. Flyway 동작 원리

### 2.1 마이그레이션 파일 네이밍 규칙

```
V1__init.sql
│ │    │
│ │    └── 설명 (자유 형식, 언더스코어로 구분)
│ └────── 언더스코어 2개 (구분자)
└──────── V + 버전 번호
```

추가 예시:
```
V1__init.sql                    ← 최초 스키마 생성
V2__add_password_to_member.sql  ← member 테이블에 password 컬럼 추가
V3__create_order_table.sql      ← order 테이블 신규 생성
```

### 2.2 실행 흐름

```
애플리케이션 시작
  │
  ├── 1. flyway_schema_history 테이블 확인 (없으면 자동 생성)
  │
  ├── 2. 이미 적용된 버전 확인
  │      예: V1 적용 완료 기록 있음
  │
  ├── 3. 아직 적용되지 않은 마이그레이션 실행
  │      예: V2, V3이 새로 있으면 순서대로 실행
  │
  ├── 4. 실행 결과를 flyway_schema_history에 기록
  │
  └── 5. Hibernate validate로 엔티티와 스키마 일치 확인
```

### 2.3 flyway_schema_history 테이블

Flyway가 자동 생성하는 메타데이터 테이블이다.

| installed_rank | version | description | script | checksum | installed_on | success |
|---|---|---|---|---|---|---|
| 1 | 1 | init | V1__init.sql | -123456 | 2026-02-25 | true |

- **checksum**: 파일 내용의 해시값. 이미 적용된 파일을 수정하면 checksum 불일치로 시작 실패
- **success**: 마이그레이션 성공 여부. 실패한 마이그레이션은 수동 해결 필요

---

## 3. 프로젝트 적용

### 3.1 의존성 추가

```gradle
dependencies {
    implementation 'org.flywaydb:flyway-core'
    implementation 'org.flywaydb:flyway-database-postgresql'
}
```

| 의존성 | 역할 |
|--------|------|
| `flyway-core` | Flyway 코어 엔진 |
| `flyway-database-postgresql` | PostgreSQL 전용 지원 (Flyway 10+에서 DB별 모듈 분리됨) |

Spring Boot는 classpath에 Flyway가 있으면 **자동으로 마이그레이션을 실행**한다.
별도의 `@EnableFlyway` 같은 설정이 필요 없다.

### 3.2 마이그레이션 스크립트

**파일 위치:** `src/main/resources/db/migration/V1__init.sql`

Spring Boot의 기본 Flyway 마이그레이션 경로가 `classpath:db/migration`이다.

```sql
CREATE TABLE category (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255)
);

CREATE TABLE member (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255),
    email VARCHAR(255)
);

CREATE TABLE product (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255),
    price INTEGER NOT NULL,
    image_url VARCHAR(255),
    category_id BIGINT,
    FOREIGN KEY (category_id) REFERENCES category (id)
);

CREATE TABLE option (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255),
    quantity INTEGER NOT NULL,
    product_id BIGINT,
    FOREIGN KEY (product_id) REFERENCES product (id)
);

CREATE TABLE wish (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    member_id BIGINT,
    product_id BIGINT,
    FOREIGN KEY (member_id) REFERENCES member (id),
    FOREIGN KEY (product_id) REFERENCES product (id)
);
```

### 3.3 H2/PostgreSQL 호환 SQL 작성 포인트

| SQL 문법 | H2 | PostgreSQL | 호환 여부 |
|----------|-----|-----------|-----------|
| `GENERATED BY DEFAULT AS IDENTITY` | O | O | 호환 |
| `BIGINT` | O | O | 호환 |
| `VARCHAR(255)` | O | O | 호환 |
| `INTEGER NOT NULL` | O | O | 호환 |
| `FOREIGN KEY ... REFERENCES` | O | O | 호환 |
| `SERIAL` | X (H2 미지원) | O | 비호환 |
| `TEXT` | O (동작은 함) | O | 호환하나 동작 미묘하게 다름 |

**핵심:** `GENERATED BY DEFAULT AS IDENTITY`는 SQL 표준이라 H2와 PostgreSQL 모두 지원한다.
PostgreSQL 전용인 `SERIAL`을 사용하면 H2에서 실패한다.

### 3.4 ddl-auto 설정 변경

| 환경 | 변경 전 | 변경 후 | 이유 |
|------|---------|---------|------|
| 기본 (`application.properties`) | 미설정 | `validate` | Flyway가 스키마 생성, Hibernate는 검증만 |
| Docker 앱 (`docker-compose.yml`) | `update` | `validate` | 동일 |
| 테스트 (`application-cucumber.properties`) | `validate` | `validate` | 변경 없음 |

**역할 분리:**

```
Flyway     → 스키마 생성/변경 (CREATE TABLE, ALTER TABLE)
Hibernate  → 스키마 검증 (엔티티와 테이블이 일치하는지 확인)
```

`validate`는 엔티티와 실제 테이블 구조가 맞는지 확인만 한다.
불일치하면 앱이 시작하지 않으므로, 마이그레이션 누락을 빠르게 감지할 수 있다.

---

## 4. Flyway와 Hibernate의 실행 순서

Spring Boot에서의 기동 순서:

```
1. DataSource 초기화 (DB 연결)
       │
2. Flyway 마이그레이션 실행
   │  └── V1__init.sql → 테이블 생성
   │
3. Hibernate 초기화
   │  └── ddl-auto=validate → 엔티티 vs 테이블 구조 검증
   │
4. 애플리케이션 시작 완료
```

**중요:** Flyway가 Hibernate보다 **먼저** 실행된다.
따라서 Hibernate의 `validate`가 실행될 시점에는 이미 테이블이 존재한다.

### 4.2 환경별 Flyway 영향 범위

마이그레이션 파일이 `src/main/resources/db/migration`에 있으므로,
**프로덕션 코드와 테스트 코드 모두** Flyway의 영향을 받는다.

| 환경 | 명령어 | DB | Flyway 동작 | 설명 |
|------|--------|-----|------------|------|
| H2 단위 테스트 | `./gradlew test` | H2 (인메모리) | V1__init.sql 실행 → H2에 테이블 생성 | 이전에는 Hibernate가 만들었지만, 이제 Flyway가 담당 |
| Docker 앱 | `./gradlew cucumberTest` | PostgreSQL | V1__init.sql 실행 → PostgreSQL에 테이블 생성 | 앱 컨테이너 시작 시 Flyway가 실행 |
| Cucumber 테스트 컨텍스트 | (위와 동일) | PostgreSQL | 이미 테이블 존재 → **스킵** | Docker 앱이 먼저 생성해둔 테이블을 그대로 사용 |

```
./gradlew test (H2)
  └── Spring 컨텍스트 시작
        ├── Flyway → H2에 V1__init.sql 실행 → 테이블 생성
        └── Hibernate validate → 통과

./gradlew cucumberTest (PostgreSQL)
  ├── Docker 앱 컨테이너 시작
  │     ├── Flyway → PostgreSQL에 V1__init.sql 실행 → 테이블 생성
  │     └── Hibernate validate → 통과
  │
  └── 테스트 컨텍스트 시작 (JDBC로 같은 DB 접속)
        ├── Flyway → flyway_schema_history 확인 → 이미 적용됨 → 스킵
        └── Hibernate validate → 통과
```

**핵심:** Flyway는 `flyway_schema_history` 테이블을 확인하여 이미 적용된 마이그레이션은 다시 실행하지 않는다.
따라서 Docker 앱과 테스트 컨텍스트가 같은 DB를 공유해도 충돌이 발생하지 않는다.

---

## 5. 스키마 변경이 필요할 때

### 5.1 컬럼 추가 예시

`Member` 엔티티에 `password` 필드를 추가한다고 가정:

**1단계:** 마이그레이션 파일 추가
```sql
-- V2__add_password_to_member.sql
ALTER TABLE member ADD COLUMN password VARCHAR(255);
```

**2단계:** 엔티티 수정
```java
@Entity
public class Member {
    // 기존 필드...
    private String password;
}
```

**3단계:** 앱 재시작 → Flyway가 V2 실행 → Hibernate가 validate 통과

### 5.2 주의사항

- 이미 적용된 마이그레이션 파일(V1__init.sql 등)은 **절대 수정하지 않는다**
  - Flyway가 checksum을 비교하므로, 수정하면 시작 실패
- 새로운 변경은 항상 **새 버전 파일**로 추가한다 (V2, V3, ...)
- 마이그레이션은 **되돌릴 수 없다** (Community 버전 기준)
  - 롤백이 필요하면 반대 작업을 하는 새 마이그레이션을 추가한다

---

## 6. 주요 의사결정과 이유

| 결정 | 이유 |
|------|------|
| H2/PostgreSQL 호환 SQL 하나로 통일 | 엔티티 5개, DB 전용 문법 불필요. 파일 2벌 관리는 오버엔지니어링 |
| `GENERATED BY DEFAULT AS IDENTITY` 사용 | SQL 표준으로 H2, PostgreSQL 모두 호환. `SERIAL`은 H2에서 실패 |
| 모든 환경에서 `ddl-auto=validate` | Flyway에 스키마 관리를 일원화. Hibernate는 검증 역할만 |
| `flyway-database-postgresql` 추가 | Flyway 10+에서 DB별 모듈이 분리됨. PostgreSQL 사용 시 필수 |

---

## 7. 탐구 질문과 답변

**Q. Flyway는 어떤 문제를 해결하는가?**

DB 스키마의 버전 관리 문제를 해결한다.
코드는 Git으로 버전 관리하지만, DB 스키마는 관리 도구 없이는 "지금 운영 DB가 어떤 상태인지" 알 수 없다.
Flyway는 마이그레이션 스크립트를 순서대로 적용하고 이력을 기록하여,
어떤 환경이든 동일한 스키마 상태를 보장한다.

**Q. ddl-auto=update를 프로덕션에서 쓰면 안 되는 이유는?**

`update`는 Hibernate가 엔티티를 보고 스키마를 자동으로 변경한다.
하지만 컬럼 삭제, 이름 변경, 타입 변경은 하지 못하고,
예상치 못한 ALTER TABLE이 실행될 수 있다.
프로덕션에서는 스키마 변경이 **명시적이고 검증된 SQL**로 실행되어야 안전하다.

**Q. Flyway 마이그레이션 파일을 수정하면 어떻게 되는가?**

Flyway는 각 마이그레이션 파일의 checksum을 `flyway_schema_history`에 저장한다.
이미 적용된 파일을 수정하면 checksum이 달라져서 앱 시작 시 에러가 발생한다.
이는 의도된 안전장치로, 이미 운영에 적용된 스키마 변경이 사후에 바뀌는 것을 방지한다.

**Q. Spring Boot에서 Flyway 설정을 별도로 하지 않아도 되는 이유는?**

Spring Boot Auto-configuration이 classpath에 Flyway가 있으면 자동으로 설정한다.
기본값:
- 마이그레이션 경로: `classpath:db/migration`
- 자동 실행: `spring.flyway.enabled=true`
- DataSource: Spring Boot의 DataSource를 자동으로 사용

따라서 의존성 추가 + SQL 파일 배치만으로 동작한다.

**Q. H2와 PostgreSQL에서 같은 SQL 파일을 쓸 수 있는 이유는?**

`GENERATED BY DEFAULT AS IDENTITY`는 SQL:2003 표준이다.
H2와 PostgreSQL 모두 이 표준을 지원하므로 동일한 DDL로 테이블을 생성할 수 있다.
단, `SERIAL`(PostgreSQL 전용)이나 `AUTO_INCREMENT`(MySQL 전용) 같은
DB별 문법을 쓰면 호환되지 않는다.

---

## 8. 트러블슈팅 키워드

```bash
# Flyway 마이그레이션 상태 확인 (DB 직접 조회)
SELECT * FROM flyway_schema_history;

# 체크섬 불일치 에러 발생 시
# → 이미 적용된 파일을 수정했는지 확인
# → 개발 환경에서는 flyway_schema_history 테이블 삭제 후 재시작

# Spring Boot Flyway 설정 오버라이드 (필요 시)
spring.flyway.enabled=true                          # 기본값
spring.flyway.locations=classpath:db/migration       # 기본값
spring.flyway.baseline-on-migrate=true               # 기존 DB에 Flyway 최초 적용 시
```
