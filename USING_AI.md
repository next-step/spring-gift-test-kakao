## 테스트 전략 수립

### 무엇을 검증할 것인가

- 처음에는 "어디까지 검증해야 하는가"에 대한 고민이 있었습니다.
- DB를 직접 조회해서 데이터를 확인할 수도 있지만, 그렇게 하면 테이블 구조가 바뀔 때 테스트도 깨집니다.
- 결국 외부에서 관찰 가능한 결과만 검증하기로 했습니다.
    - **HTTP 계약**: 상태 코드, 응답 body 필드
    - **관찰 가능한 상태 변화**: POST 후 GET으로 반영 여부 확인
    - **사이드 이펙트**: API를 통한 간접 검증 (예: 재고 소진 후 재요청 시 실패 여부)

### 테스트 데이터를 어떻게 준비할 것인가

- 테스트 데이터 준비 방식으로 SQL 직접 삽입, API 호출, Fixture 등을 비교했습니다.
    - **SQL**: 빠르지만 스키마에 직접 결합되어 구조 변경 시 테스트가 깨집니다.
    - **API 호출**: 실제 흐름과 가깝지만, API가 없는 도메인(Option, Member)은 준비할 수 없습니다.
    - **Fixture**: 비즈니스 로직 수준에서 객체를 생성하므로 테스트 의도가 명확하고, 도메인 규칙을 우회하지 않아 비정상 데이터가 만들어질 위험이 없습니다.
- API가 있는 도메인(Category, Product)은 API 호출로, API가 없는 도메인(Option, Member)은 Fixture로 준비하는 방식을 선택했습니다.

### 테스트 격리

- `DatabaseCleanup`을 만들어 `@BeforeEach`에서 전체 테이블을 truncate하여 테스트 간 데이터 격리를 보장했습니다.

## 작성 과정에서의 고민과 결정

### 중복 테스트 제거

- `생성` 행위에서 "생성 후 조회에 포함되는지 확인"하는 테스트를 이미 만들었는데, `조회` 행위에서 "여러 개 생성 후 전체 조회"를 또 만드는 것은 중복이라 판단하여 제거했습니다.

### `@RequestBody` 누락 발견 및 수정

- `CategoryRestController`와 `ProductRestController`의 create 메서드에 `@RequestBody`가 없었습니다.
- 의도된 설계인지 검토한 결과, `@RestController` + `/api/` 경로 + JSON 응답 + Thymeleaf 템플릿 없음 → REST API로서 JSON 요청이 자연스럽다고 판단하여 코드에
  `@RequestBody`를 추가했습니다.

### 선물 전달 사이드 이펙트 검증

- 선물 전달 성공(200) 확인만으로는 재고가 실제로 차감되었는지 알 수 없습니다.
- Option 조회 API가 없으므로 DB 직접 조회 대신, 재고를 전부 소진한 뒤 추가 요청이 실패(500)하는지로 간접 검증했습니다.
- 수신자 상태는 `FakeGiftDelivery`가 콘솔 출력만 하고 확인할 API가 없어 검증 대상에서 제외했습니다.
