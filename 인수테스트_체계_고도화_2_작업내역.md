# 인수 테스트 체계 고도화 2단계 — 작업 내역

## 목차
1. [작업 개요](#작업-개요)
2. [요구사항 1: Cucumber BDD 적용](#요구사항-1-cucumber-bdd-적용)
3. [요구사항 2: PostgreSQL + Docker Compose 통합](#요구사항-2-postgresql--docker-compose-통합)
4. [요구사항 3: Application 컨테이너화](#요구사항-3-application-컨테이너화)
5. [트러블슈팅](#트러블슈팅)
6. [고민해볼 점](#고민해볼-점)
7. [Cucumber 애노테이션 설명 (@만일/@그리고 vs @Test)](#cucumber-애노테이션-설명)
8. [커밋 이력](#커밋-이력)
9. [최종 파일 구조](#최종-파일-구조)

---

## 작업 개요

기존 RestAssured 기반 인수 테스트(7개 행위)를 Cucumber BDD 시나리오로 전환하고,
PostgreSQL + Docker Compose로 프로덕션 유사 환경을 구축한 뒤,
애플리케이션까지 컨테이너화하여 End-to-End 테스트 파이프라인을 완성했다.

### 작업 전 상태
- `CategoryBehaviorTest` (1개 행위), `ProductBehaviorTest` (2개 행위), `GiftBehaviorTest` (4개 행위)
- H2 in-memory DB, `@SpringBootTest(RANDOM_PORT)` + RestAssured로 내장 서버 호출
- `./gradlew test`로만 실행

### 작업 후 상태
- 기존 RestAssured 테스트: 그대로 유지 (`./gradlew test` → H2 in-memory)
- Cucumber BDD 테스트: 6개 시나리오 추가 (`./gradlew cucumberTest` → PostgreSQL Docker)
- 전체 E2E: App + PostgreSQL 모두 Docker 컨테이너 (`./gradlew dockerBuild → dockerUp → cucumberTest → dockerDown`)

---

## 요구사항 1: Cucumber BDD 적용

### 무엇을 했는가

**1) Gradle 의존성 추가** (`build.gradle`)

```groovy
// Cucumber BDD
testImplementation platform('io.cucumber:cucumber-bom:7.18.1')
testImplementation 'io.cucumber:cucumber-java'
testImplementation 'io.cucumber:cucumber-spring'
testImplementation 'io.cucumber:cucumber-junit-platform-engine'
testImplementation 'org.junit.platform:junit-platform-suite'
```

- `cucumber-bom`으로 버전 통합 관리
- `cucumber-spring`으로 Spring Boot DI 통합
- `cucumber-junit-platform-engine`으로 JUnit Platform에서 Cucumber 실행
- `junit-platform-suite`로 Suite API 사용

**2) Cucumber 실행 러너 구성**

```java
// src/test/java/gift/cucumber/CucumberTest.java
@Suite
@IncludeEngines("cucumber")
@SelectClasspathResource("features")
public class CucumberTest {}
```

- `@Suite` + `@IncludeEngines("cucumber")`로 Gradle `test` 태스크에서 Cucumber를 자동 실행
- `@SelectClasspathResource("features")`로 feature 파일 위치 지정

**3) Spring Boot 통합 설정**

```java
// src/test/java/gift/cucumber/CucumberSpringConfiguration.java
@CucumberContextConfiguration
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.NONE)
@ActiveProfiles("cucumber")
public class CucumberSpringConfiguration {}
```

- `@CucumberContextConfiguration`: Cucumber와 Spring ApplicationContext를 연결
- `webEnvironment = NONE`: 내장 서버를 띄우지 않음 (Docker 컨테이너 App에 요청하므로)
- `@ActiveProfiles("cucumber")`: PostgreSQL 전용 설정 활성화

**4) ScenarioContext — 시나리오 스코프 빈**

```java
// src/test/java/gift/cucumber/ScenarioContext.java
@Component
@ScenarioScope
public class ScenarioContext {
    private final Map<String, Object> context = new HashMap<>();

    public void set(String key, Object value) { context.put(key, value); }
    public <T> T get(String key, Class<T> type) { return type.cast(context.get(key)); }
}
```

- Step Definition 클래스에 멤버 필드로 상태를 저장하면 시나리오 간 오염 위험
- `@ScenarioScope` 빈으로 시나리오마다 새 인스턴스 생성 → 격리 보장
- `lastResponse`, `senderId`, `receiverId`, `optionId` 등을 key-value로 공유

**5) Feature 파일 작성 (한글 Gherkin)**

총 3개 feature 파일, 6개 시나리오:

| Feature 파일 | 시나리오 수 | 검증 행위 |
|---|---|---|
| `gift.feature` | 4개 | 선물 성공/재고부족/미존재옵션/미존재회원 |
| `category.feature` | 1개 | 카테고리 생성 → 목록 조회 |
| `product.feature` | 1개 | 미존재 카테고리로 상품 생성 실패 |

예시 (`gift.feature`):

```gherkin
# language: ko
기능: 선물하기
  카카오 선물하기 사용자가 친구에게 선물을 보낼 수 있다

  배경:
    조건 보내는 회원과 받는 회원이 존재한다

  시나리오: 재고가 충분하면 선물하기가 성공하고 재고가 감소한다
    조건 "테스트옵션" 옵션의 재고가 10개 있다
    만일 회원이 "테스트옵션" 3개를 선물한다
    그러면 선물하기가 성공한다
    그리고 "테스트옵션" 옵션의 재고가 7개이다
```

- `# language: ko`로 한글 키워드 활성화
- `배경`(Background)으로 공통 전제조건 추출
- 비즈니스 용어 사용 (HTTP/JSON 기술 용어 배제)

**6) Step Definitions 구현**

3개 Step Definition 클래스:

- `GiftStepDefinitions.java`: 선물 관련 8개 step
  - Given: Repository로 직접 테스트 데이터 생성 (Category → Product → Option → Member)
  - When: RestAssured로 `POST /api/gifts` 호출
  - Then: HTTP 상태코드 + DB 재고 검증
- `CategoryStepDefinitions.java`: 카테고리 관련 5개 step
  - When: RestAssured로 JSON `POST /api/categories`, `GET /api/categories`
  - Then: HTTP 응답 body에서 카테고리 이름 검증
- `ProductStepDefinitions.java`: 상품 관련 4개 step
  - When: RestAssured로 formParam `POST /api/products`, `GET /api/products`
  - Then: HTTP 상태코드 + 빈 목록 검증

**7) 시나리오 간 데이터 격리**

```java
// CucumberHooks.java
@Before
public void setUp() {
    RestAssured.baseURI = "http://localhost:28080";
    jdbcTemplate.execute("TRUNCATE TABLE option, wish, product, category, member CASCADE");
}
```

- 매 시나리오 시작 전 전체 테이블 TRUNCATE
- `CASCADE`로 FK 의존성 해결
- 시나리오 실행 순서에 무관하게 독립 실행 보장

### Cucumber 설정 파일

```properties
# src/test/resources/cucumber.properties
cucumber.glue=gift.cucumber
cucumber.plugin=pretty
cucumber.publish.quiet=true
```

---

## 요구사항 2: PostgreSQL + Docker Compose 통합

### 무엇을 했는가

**1) docker-compose.yml — PostgreSQL 서비스 정의**

```yaml
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: gift_test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test -d gift_test"]
      interval: 3s
      timeout: 3s
      retries: 10
```

- `postgres:16-alpine`: 경량 이미지
- `healthcheck`: `pg_isready`로 DB 준비 상태를 확인 → 테스트가 DB 미준비 상태에서 시작하는 것을 방지
- `ports: "5432:5432"`: Host에서 JDBC로 직접 접근 가능 (테스트 데이터 격리용)

**2) Spring Profile 분리**

```properties
# src/test/resources/application-cucumber.properties
spring.datasource.url=jdbc:postgresql://localhost:5432/gift_test
spring.datasource.username=test
spring.datasource.password=test
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
```

- `@ActiveProfiles("cucumber")`로 활성화
- `globally_quoted_identifiers=true`: PostgreSQL 예약어(`option`) 충돌 해결
- 기존 `./gradlew test`는 H2 in-memory를 그대로 사용 (기본 프로파일)

**3) Gradle Task: cucumberTest**

```groovy
tasks.register('composeUp', Exec) {
    commandLine 'docker', 'compose', 'up', '-d', 'postgres', '--wait'
}

tasks.register('composeDown', Exec) {
    commandLine 'docker', 'compose', 'down', '-v'
}

tasks.register('cucumberTest', Test) {
    useJUnitPlatform()
    include 'gift/cucumber/**'
    dependsOn 'composeUp'
    finalizedBy 'composeDown'
}
```

- `composeUp`: PostgreSQL만 시작 (`postgres` 서비스 지정, `--wait`로 healthcheck 통과 대기)
- `cucumberTest`: Cucumber 테스트만 실행 (`include 'gift/cucumber/**'`)
- `finalizedBy 'composeDown'`: 테스트 성공/실패와 관계없이 반드시 정리

### 네트워크 구조 (요구사항 2 시점)

```
Host (테스트 코드)
  ├── RestAssured → localhost:28080 → (아직 App 컨테이너 없음, 요구사항 3에서 추가)
  └── JdbcTemplate → localhost:5432 → Docker PostgreSQL
```

---

## 요구사항 3: Application 컨테이너화

### 무엇을 했는가

**1) Dockerfile (Multi-stage build)**

```dockerfile
# Builder stage
FROM eclipse-temurin:21-jdk AS builder
WORKDIR /app
COPY gradle gradle
COPY gradlew build.gradle settings.gradle ./
COPY src src
RUN chmod +x gradlew && ./gradlew bootJar -x test --no-daemon

# Runtime stage
FROM eclipse-temurin:21-jre-alpine
WORKDIR /app
COPY --from=builder /app/build/libs/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
```

- **Builder stage**: `eclipse-temurin:21-jdk` (non-alpine) — ARM(aarch64) Mac에서 alpine JDK가 SIGSEGV 발생하여 non-alpine 사용
- **Runtime stage**: `eclipse-temurin:21-jre-alpine` — 경량 JRE 이미지 (런타임은 alpine 문제 없음)
- `bootJar -x test`: 테스트 제외하고 JAR만 빌드

**2) docker-compose.yml — app 서비스 추가**

```yaml
app:
  build: .
  ports:
    - "28080:8080"
  depends_on:
    postgres:
      condition: service_healthy
  environment:
    SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/gift_test
    SPRING_DATASOURCE_USERNAME: test
    SPRING_DATASOURCE_PASSWORD: test
    SPRING_DATASOURCE_DRIVER_CLASS_NAME: org.postgresql.Driver
    SPRING_JPA_DATABASE_PLATFORM: org.hibernate.dialect.PostgreSQLDialect
    SPRING_JPA_HIBERNATE_DDL_AUTO: create-drop
    SPRING_JPA_PROPERTIES_HIBERNATE_GLOBALLY_QUOTED_IDENTIFIERS: "true"
```

- `depends_on: condition: service_healthy`: PostgreSQL healthcheck 통과 후 App 시작
- 컨테이너 내부에서 DB 접근 주소는 `postgres:5432` (Docker 네트워크의 service name이 hostname)
- Host에서 App 접근은 `localhost:28080` (포트 매핑 `28080:8080`)

**3) Gradle Task 추가**

```groovy
tasks.register('dockerBuild', Exec) {
    commandLine 'docker', 'compose', 'build'
}

tasks.register('dockerUp', Exec) {
    commandLine 'docker', 'compose', 'up', '-d', '--wait'
}

tasks.register('dockerDown', Exec) {
    commandLine 'docker', 'compose', 'down', '-v'
}
```

**4) webEnvironment = NONE 적용**

- `CucumberSpringConfiguration`에서 `webEnvironment = SpringBootTest.WebEnvironment.NONE` 설정
- 테스트 프로세스에서 내장 서버를 띄우지 않음
- RestAssured가 `http://localhost:28080` (Docker App 컨테이너)으로 HTTP 요청

### 최종 E2E 아키텍처

```
┌─────────────────────────────────────────────────┐
│ Host (개발자 머신)                                │
│                                                   │
│  ┌──────────────┐                                │
│  │ Gradle       │                                │
│  │ cucumberTest │                                │
│  │              │                                │
│  │ RestAssured ─┼── HTTP ──→ localhost:28080     │
│  │ JdbcTemplate ┼── JDBC ──→ localhost:5432      │
│  └──────────────┘                                │
│                                                   │
├─────────────────── Docker Network ───────────────┤
│                                                   │
│  ┌──────────┐         ┌──────────────┐           │
│  │ app      │── JDBC →│ postgres     │           │
│  │ :8080    │         │ :5432        │           │
│  │ ↕28080   │         │ ↕5432        │           │
│  └──────────┘         └──────────────┘           │
│                                                   │
└─────────────────────────────────────────────────┘
```

- **테스트(Host)** → HTTP → `localhost:28080` → **App(Container)**
- **테스트(Host)** → JDBC → `localhost:5432` → **PostgreSQL(Container)** (DB 초기화용)
- **App(Container)** → JDBC → `postgres:5432` → **PostgreSQL(Container)**

---

## 트러블슈팅

### 1. Docker build SIGSEGV (ARM Mac + Alpine JDK)

**증상**: `eclipse-temurin:21-jdk-alpine` 이미지에서 `./gradlew bootJar` 실행 시 SIGSEGV 발생

```
# A fatal error has been detected by the Java Runtime Environment:
# SIGSEGV (0xb) at pc=..., pid=..., tid=...
# Problematic frame:
# C  [libnative-platform-file-events.so+0x...]
```

**원인**: Alpine Linux의 musl libc와 ARM(aarch64) 아키텍처에서 Gradle의 native-platform 라이브러리가 충돌

**해결**: Builder stage만 non-alpine 이미지로 변경

```dockerfile
# Before (SIGSEGV)
FROM eclipse-temurin:21-jdk-alpine AS builder

# After (정상)
FROM eclipse-temurin:21-jdk AS builder
```

Runtime stage는 `eclipse-temurin:21-jre-alpine` 그대로 유지 (Gradle 미사용이므로 문제 없음)

### 2. PostgreSQL 예약어 `option` 충돌

**증상**: `option` 테이블에 대한 DDL/DML 실행 시 PostgreSQL 문법 오류

**원인**: `option`은 PostgreSQL 예약어. H2에서는 문제없었지만 PostgreSQL에서는 식별자를 따옴표로 감싸야 함

**해결**: Hibernate 전역 식별자 인용 활성화 (프로덕션 코드 변경 없이)

```properties
# application-cucumber.properties
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
```

```yaml
# docker-compose.yml (app 서비스)
SPRING_JPA_PROPERTIES_HIBERNATE_GLOBALLY_QUOTED_IDENTIFIERS: "true"
```

### 3. CategoryBehaviorTest 실패 (이전 작업에서 수정)

**증상**: `should_create_category_and_return_in_list` 테스트 실패 (HTTP 500)

**원인**: `CategoryRestController`에 `@RequestBody`가 추가되었지만, 테스트는 `formParam()`으로 form-urlencoded 전송

**해결**: 테스트를 JSON 요청으로 변경

```java
// Before (실패)
RestAssured.given()
    .formParam("name", "테스트카테고리")
    .when().post("/api/categories")

// After (성공)
RestAssured.given()
    .contentType(ContentType.JSON)
    .body("{\"name\": \"테스트카테고리\"}")
    .when().post("/api/categories")
```

---

## 고민해볼 점

`인수테스트_체계_고도화_2.md`에서 제시한 관점별 고민 포인트를 정리합니다.

### 전체 미션 관점

| 질문 | 현재 답변 |
|---|---|
| 다른 개발자도 동일한 명령으로 실행 가능한가? | `./gradlew test` (H2), `./gradlew cucumberTest` (PostgreSQL), `./gradlew dockerBuild && dockerUp && cucumberTest && dockerDown` (E2E). Docker만 설치되어 있으면 동일하게 실행 가능 |
| 실행 방법이 충분히 단순한가? | Gradle Task로 감싸서 단일 명령. README에도 복사/붙여넣기 가능한 형태로 문서화 |
| 실패했을 때 원인을 쉽게 파악할 수 있는가? | `docker ps`, `docker logs <container>` 안내. Cucumber `pretty` 플러그인으로 시나리오별 성공/실패 출력 |

### 요구사항 1 (Cucumber) 관점

| 질문 | 현재 답변 |
|---|---|
| 비개발자가 시나리오를 읽고 이해할 수 있는가? | 한글 Gherkin + 비즈니스 용어 사용. "회원이 '테스트옵션' 3개를 선물한다"처럼 자연어에 가까움 |
| Step Definitions는 재사용 가능하게 작성되었는가? | 파라미터화된 step (`{string}`, `{int}`) 사용. 예: `"{string} 옵션의 재고가 {int}개 있다"` → 옵션 이름과 수량을 자유롭게 변경 가능 |
| 시나리오 간 데이터가 격리되는가? | `@Before` hook에서 `TRUNCATE ... CASCADE` 수행 + `@ScenarioScope` 빈으로 상태 격리 |

**추가 탐구 질문:**

- **Gherkin의 Given/When/Then은 무엇을 의미하는가?**
  - Given(조건): 시나리오의 초기 상태/전제조건. "~가 존재한다"
  - When(만일): 사용자가 수행하는 행동. "~을 한다"
  - Then(그러면): 행동의 기대 결과. "~가 된다"
  - And(그리고): 이전 키워드의 연장

- **Step Definitions에서 파라미터를 어떻게 추출하는가?**
  - `{string}` → `String` 파라미터, `{int}` → `int` 파라미터
  - Cucumber Expression으로 Gherkin 문장의 동적 부분을 메서드 파라미터로 매핑

- **시나리오 간 Response 객체를 어떻게 공유하는가?**
  - `ScenarioContext` 빈에 `context.set("lastResponse", response)` → 다음 step에서 `context.get("lastResponse", Response.class)`

- **`@Before` hook은 언제 실행되는가?**
  - 각 시나리오(Scenario)가 시작되기 전에 실행됨. Background 단계보다도 먼저 실행

### 요구사항 2 (PostgreSQL) 관점

| 질문 | 현재 답변 |
|---|---|
| 왜 H2 대신 PostgreSQL을 사용하는가? | **Production Parity** — 프로덕션 환경과 동일한 DB로 테스트해야 DB 방언 차이(예약어, 타입, 함수)로 인한 배포 후 장애를 사전에 발견 가능 |
| Production Parity란 무엇인가? | 개발/테스트/프로덕션 환경을 최대한 동일하게 유지하는 원칙. Twelve-Factor App의 원칙 중 하나 |
| 테스트 실패 시에도 DB가 정리되는가? | `cucumberTest` 태스크에 `finalizedBy 'composeDown'` 적용 → 성공/실패 무관하게 `docker compose down -v` 실행 |

**추가 탐구 질문:**

- **Docker Compose의 `services`, `volumes`는 무엇인가?**
  - `services`: 각 컨테이너(postgres, app 등) 정의
  - `volumes`: 데이터 영속 저장소. 현재는 `-v` 옵션으로 매번 삭제하여 깨끗한 상태 보장

- **Health check는 왜 필요한가?**
  - PostgreSQL 컨테이너가 시작되어도 DB가 연결을 받을 준비가 안 된 상태일 수 있음
  - `pg_isready`로 실제 연결 가능 시점까지 대기 → 테스트 안정성 확보

- **H2 단위 테스트와 PostgreSQL 통합 테스트를 어떻게 분리하는가?**
  - `./gradlew test` → 기존 `*BehaviorTest` 클래스 (H2 in-memory, 기본 프로파일)
  - `./gradlew cucumberTest` → `gift/cucumber/**` 클래스만 실행 (PostgreSQL, cucumber 프로파일)
  - Spring Profile (`@ActiveProfiles("cucumber")`)로 datasource 설정 분리

### 요구사항 3 (Docker) 관점

| 질문 | 현재 답변 |
|---|---|
| 왜 애플리케이션까지 컨테이너로 실행하는가? | 로컬 JDK 버전/OS 차이 등의 환경 변수를 제거. 배포 형태(JAR in Docker)와 동일한 구조에서 테스트 |
| 테스트는 어디서 실행되고, 애플리케이션은 어디서 실행되는가? | 테스트: Host (Gradle). 애플리케이션: Docker 컨테이너 (localhost:28080). DB 초기화: Host → localhost:5432 |
| Multi-stage build를 사용하는 이유는? | Builder stage에서 Gradle 빌드 수행 후, Runtime stage에는 JAR만 복사 → 최종 이미지에 JDK, 소스코드, Gradle 미포함으로 이미지 크기 대폭 감소 |

**추가 탐구 질문:**

- **Docker 네트워크에서 service name이 어떻게 hostname이 되는가?**
  - Docker Compose는 같은 Compose 파일의 서비스들을 동일 네트워크에 배치
  - 서비스 이름(`postgres`)이 DNS hostname으로 등록됨
  - `app` 컨테이너에서 `postgres:5432`로 접근 가능

- **컨테이너 내부에서는 `postgres:5432`, 테스트에서는 `localhost:28080`인 이유는?**
  - 컨테이너 간: Docker 내부 네트워크 사용 → service name으로 접근
  - Host → 컨테이너: port mapping 사용 → `localhost:{매핑된 포트}`

- **`webEnvironment = NONE`을 사용하는 이유는?**
  - Cucumber 테스트는 Docker 컨테이너의 App에 HTTP 요청을 보냄
  - Host에서 내장 서버를 또 띄우면 "어느 서버에 요청하는 건지" 혼동 발생
  - `NONE`으로 embedded 서버를 제거하여 명확하게 컨테이너 App만 호출

- **왜 JdbcTemplate이 필요한가?**
  - 시나리오 시작 전 DB 초기화(TRUNCATE)를 위해 Host에서 PostgreSQL에 직접 접근
  - `webEnvironment = NONE`이지만 Spring ApplicationContext는 로드되므로 JdbcTemplate DI 가능

- **`.dockerignore`는 왜 필요한가?**
  - Docker build context에서 불필요한 파일(`.gradle`, `build`, `.git` 등)을 제외
  - 빌드 속도 향상 + 이미지 크기 감소

---

## Cucumber 애노테이션 설명

### `@만일`, `@그리고` 등은 무엇인가?

Cucumber의 Step Definition 클래스에는 **`@Test` 애노테이션이 없다**. 이것은 JUnit 테스트가 아니기 때문이다.

```java
import io.cucumber.java.ko.만일;
import io.cucumber.java.ko.그러면;
import io.cucumber.java.ko.그리고;
import io.cucumber.java.ko.조건;

public class GiftStepDefinitions {

    @조건("보내는 회원과 받는 회원이 존재한다")  // = @Given
    public void 보내는_회원과_받는_회원이_존재한다() { ... }

    @만일("회원이 {string} {int}개를 선물한다")   // = @When
    public void 회원이_N개를_선물한다(String optionName, int quantity) { ... }

    @그러면("선물하기가 성공한다")               // = @Then
    public void 선물하기가_성공한다() { ... }

    @그리고("{string} 옵션의 재고가 {int}개이다") // = @And
    public void 옵션의_재고가_N개이다(String optionName, int expectedQuantity) { ... }
}
```

### 이 애노테이션들은 왜 존재하는가?

| 패키지 | 애노테이션 | Gherkin 키워드 |
|---|---|---|
| `io.cucumber.java.en` | `@Given`, `@When`, `@Then`, `@And` | Given, When, Then, And |
| `io.cucumber.java.ko` | `@조건`, `@만일`, `@그러면`, `@그리고` | 조건, 만일, 그러면, 그리고 |

- Feature 파일에 `# language: ko`를 선언하면 Gherkin이 한글 키워드를 인식
- Step Definition도 `io.cucumber.java.ko` 패키지의 한글 애노테이션 사용 가능
- **`@조건` = `@Given`**, **`@만일` = `@When`**, **`@그러면` = `@Then`**, **`@그리고` = `@And`**

### 그러면 테스트는 어떻게 실행되는가?

Cucumber 테스트의 실행 흐름:

```
1. CucumberTest.java (@Suite + @IncludeEngines("cucumber"))
   → JUnit Platform이 Cucumber 엔진을 로드

2. Cucumber 엔진이 features/*.feature 파일을 파싱
   → 각 시나리오의 한글 스텝("만일 회원이 ...")을 인식

3. cucumber.glue=gift.cucumber 설정을 보고
   → Step Definition 클래스들을 스캔

4. Feature의 각 스텝 문장을 → @만일/@그러면 등의 패턴과 매칭
   → 해당 Java 메서드를 순서대로 실행

5. 모든 시나리오의 모든 스텝이 성공하면 테스트 통과
```

즉, **`@Test`는 JUnit이 직접 실행하는 메서드를 표시**하지만, **`@만일`/`@그리고`는 Cucumber 엔진이 Feature 파일의 스텝과 매칭하여 실행**하는 메서드를 표시한다. 테스트의 진입점은 `CucumberTest.java`의 `@Suite` 애노테이션이다.

---

## 커밋 이력

| 커밋 | 메시지 | 내용 |
|---|---|---|
| `81953cd` | `chore(test): add cucumber bdd acceptance tests` | 요구사항 1 완료 — Cucumber 의존성, feature 파일, step definitions, 러너, Spring 통합 |
| `ff7b2a6` | `test: run cucumber tests with postgres via docker compose` | 요구사항 2 완료 — docker-compose.yml (postgres), application-cucumber.properties, cucumberTest Gradle task |
| `87207f7` | `chore(docker): run app + postgres and execute cucumber e2e tests` | 요구사항 3 완료 — Dockerfile, app 서비스, dockerBuild/dockerUp/dockerDown tasks |
| `a4c7681` | `docs: update README with test execution instructions` | README.md에 실행 방법 + 트러블슈팅 문서화 |

---

## 최종 파일 구조

```
spring-gift-test-kakao/
├── Dockerfile                          # Multi-stage build
├── .dockerignore                       # Docker build context 제외 목록
├── docker-compose.yml                  # postgres + app 서비스
├── build.gradle                        # Cucumber 의존성 + Gradle tasks
├── README.md                           # 실행 방법 문서
│
├── src/test/resources/
│   ├── cucumber.properties             # Cucumber 글로벌 설정
│   ├── application-cucumber.properties # PostgreSQL 연결 설정
│   └── features/
│       ├── gift.feature                # 선물하기 4개 시나리오
│       ├── category.feature            # 카테고리 1개 시나리오
│       └── product.feature             # 상품 1개 시나리오
│
└── src/test/java/gift/
    ├── CategoryBehaviorTest.java       # 기존 RestAssured 테스트 (H2)
    ├── ProductBehaviorTest.java        # 기존 RestAssured 테스트 (H2)
    ├── GiftBehaviorTest.java           # 기존 RestAssured 테스트 (H2)
    │
    └── cucumber/
        ├── CucumberTest.java           # @Suite 러너 (진입점)
        ├── CucumberSpringConfiguration.java  # Spring 통합 설정
        ├── CucumberHooks.java          # @Before: DB 초기화 + RestAssured 설정
        ├── ScenarioContext.java         # @ScenarioScope 상태 공유 빈
        └── steps/
            ├── GiftStepDefinitions.java      # 선물 Step Definitions
            ├── CategoryStepDefinitions.java  # 카테고리 Step Definitions
            └── ProductStepDefinitions.java   # 상품 Step Definitions
```

---

## 검증 명령어 요약

```bash
# 1. 기존 단위/인수 테스트 (H2 in-memory)
./gradlew test

# 2. Cucumber BDD + PostgreSQL (Docker Compose)
./gradlew cucumberTest

# 3. 전체 E2E (App + PostgreSQL 모두 Docker)
./gradlew dockerBuild
./gradlew dockerUp
curl http://localhost:28080/api/categories  # 앱 응답 확인
./gradlew cucumberTest
./gradlew dockerDown
```
