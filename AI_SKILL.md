# AI 활용 방법 문서

## 활용한 커스텀 스킬

Claude Code의 커스텀 스킬(`.claude/commands/`)을 정의하여 반복 작업을 자동화했다.

### /test - 테스트 실행 및 결과 정리

```
/test
```

- `./gradlew test`를 실행하고 결과를 정리된 형식으로 보여준다.
- 전체/성공/실패/스킵 수를 요약하고, 실패 시 각 테스트의 실패 원인을 간략히 출력한다.
- **사용 시점:** 코드 수정 후 빠르게 테스트 상태를 확인할 때

### /test-fix - 실패한 테스트 자동 분석 및 수정

```
/test-fix
```

- 테스트를 실행하여 실패 항목을 식별한다.
- 테스트 코드와 프로덕션 코드를 함께 읽고 실패 원인을 판단한다.
- 프로덕션 버그이면 프로덕션 코드를, 테스트 기대값 오류이면 테스트를 수정한다.
- 수정 후 재실행하여 통과를 확인한다.
- **사용 시점:** 리팩터링 후 깨진 테스트를 빠르게 복구할 때

### /qa - 빌드 → 테스트 → 수정 전체 파이프라인

```
/qa
```

- 빌드(컴파일) → 테스트 실행 → 실패 분석/수정을 순차적으로 수행한다.
- 모든 단계를 통과할 때까지 반복하고, 최종 결과를 PASS/FAIL로 보고한다.
- **사용 시점:** 작업 완료 후 전체 품질을 한번에 점검할 때